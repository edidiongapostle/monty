#include "monty.h"

/**
 * nop - swap the top two element
 * @stack: the stack to work with
 * @line_number: where the error occured
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
